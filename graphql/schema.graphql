type Champion {
	id: String!
	name: String!
	key: String!
	title: String!
	tags: [String]!
	blurb: String!
}

type Query {
	champions: [Champion]!
	championId(id: String!): Champion
	championKey(key: String!): Champion
	championImage(key: String!): String
}
